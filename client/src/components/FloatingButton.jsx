import React from "react";
import { Button } from "react-bootstrap";

export const FloatingButton = () => {
  return (
    <Button variant="primary" className="floating-button rounded-pill">
      +
    </Button>
  );
};
